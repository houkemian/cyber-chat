# 2000.exe 项目全局开发规约 (Flutter 客户端)

## 1. 项目背景与视觉灵魂 (Vibe)
- **项目名称**：2000.exe (赛博树洞 / Cyber Dream Space)
- **核心视觉**：Y2K (千禧年复古赛博风)、16-bit 像素风、废土黑客终端。
- **UI 规范**：
  - 严禁使用任何现代的圆角 (`borderRadius: BorderRadius.circular`)，所有边框必须是直角 (0px)。
  - 严禁使用 Material Design 默认的阴影 (Elevation)，阴影必须是硬边缘的纯色块。
  - 核心色彩：纯黑 (#000000), 霓虹紫 (#bc00ff), 赛博蓝 (#00f0ff), 荧光绿 (#39ff14)。

## 2. 技术栈与架构约束 (Tech Stack)
- **目录结构**：
  - 前端层使用 Flutter + Dart 编写，位于 `/flutter_client` 目录。
  - 后端接口依然遵守原有规范，位于 `/backend` 目录。
- **Flutter UI 规范 (最高优先级)**：
  - 布局：优先使用 `Container`, `Column`, `Row`, `Stack` 构建。
  - 字体：强制使用全局自定义字体 `PixelFont`，并禁用字体的抗锯齿 (设为 `FontFeature.disable_anti_aliasing` 类似效果，或明确指定字号为像素字体的原生整数倍)。
  - 图像渲染：所有图片和 Icon 渲染必须设置 `filterQuality: FilterQuality.none`，确保图片放大后是清晰的马赛克像素块，而不是模糊的平滑边缘。

## 3. Cursor 行为准则 (AI Instructions)
- **禁止 Material 化**：在生成 UI 代码时，绝对不要使用 `Card`, `AppBar`, `ElevatedButton` 等带有强烈现代 Android 风格的自带组件。必须使用 `GestureDetector` 包裹 `Container` 手工捏造复古按钮。
- **组件拆分**：将复杂的 UI 拆分为纯函数式的 StatelessWidget，保持 `build` 方法简短。
- **状态管理**：使用 Provider 或 ValueNotifier 处理本地状态。
- **跨平台对齐**：当你修改 Flutter 网络请求时，必须参考 `/backend/main.py` 里的 Pydantic 模型确保字段一致。

## 4. 常用 UI 样式代码片段 (Flutter Snippets)
- **Windows 95 浮雕边框**：
  ```dart
  BoxDecoration(
    color: Color(0xFFC0C0C0),
    border: Border(
      top: BorderSide(color: Colors.white, width: 2),
      left: BorderSide(color: Colors.white, width: 2),
      bottom: BorderSide(color: Color(0xFF424242), width: 2),
      right: BorderSide(color: Color(0xFF424242), width: 2),
    ),
  )

  荧光发光文本 (Neon Glow)：
  TextStyle(
    color: Color(0xFF39FF14),
    fontFamily: 'PixelFont',
    shadows: [
      Shadow(color: Color(0xFF39FF14), blurRadius: 10),
      Shadow(color: Color(0xFF39FF14), blurRadius: 20),
    ],
  )